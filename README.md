# AngularServerDrivenUi

Projeto que utiliza a solução server-driven-ui para renderização do front a partir de um JSON de configuração.

## Rodar o projeto

Instale as dependencias: `npm install`

Inicie o servidor de mock: `npm run mock`

Inicie a aplicação angular: `npm run start`
